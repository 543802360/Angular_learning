import { CesiumViewerDirective } from './cesium-viewer.directive';

describe('CesiumViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new CesiumViewerDirective();
    expect(directive).toBeTruthy();
  });
});
