import { MousepositionDirective } from './mouseposition.directive';

describe('MousepositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MousepositionDirective();
    expect(directive).toBeTruthy();
  });
});
